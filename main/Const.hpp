#pragma once

namespace NekoRay {
    namespace DomainMatcher {
        enum DomainMatcher {
            DEFAULT,
            MPH,
        };
    }

    namespace SniffingMode {
        enum SniffingMode {
            DISABLE,
            TO_DNS,
            TO_DESTINATION,
        };
    }
}

